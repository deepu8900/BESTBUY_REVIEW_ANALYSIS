Execution Note:

The working implementation of scraping and sentiment analysis is provided
in the Python scripts inside the /scripts folder.

The Jupyter notebook is included only as a reference version of the workflow.
All outputs (CSV files) were generated successfully using the .py scripts.